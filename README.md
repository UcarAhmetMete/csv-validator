# CSV Validator (Parsing + Compliance Checks)

Small Python tool that parses a CSV file, runs basic data-quality checks (missing values, duplicates, sanity rules) and outputs a **JSON report**.

## Run
```bash
python validate_csv.py
